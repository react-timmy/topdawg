{
  "name": "filmsort-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — API proxy for FilmSort (TMDB + Gemini)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets": "echo 'Run: wrangler secret put TMDB_API_KEY && wrangler secret put GEMINI_API_KEY'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
